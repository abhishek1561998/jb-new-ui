// src/app/[countryCode]/account/(main)/account/@login/auth/page.tsx
"use client"

import AuthPage from "../auth/page"
// If you don't have a separate component, import your previous unified UI directly.

export default function LoginAuthRoute() {
  return <AuthPage />
}
